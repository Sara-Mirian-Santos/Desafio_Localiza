services:
  pipeline:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: fraud-credit-pipeline

    volumes:
      - ./data:/app/data
      - ./reports:/app/reports

    command: python -m src.pipeline